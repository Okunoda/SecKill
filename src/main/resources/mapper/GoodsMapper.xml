<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="website.okunoda.secondtokill.mapper.GoodsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="website.okunoda.secondtokill.pojo.Goods">
        <id column="id" property="id"/>
        <result column="goods_name" property="goodsName"/>
        <result column="goods_title" property="goodsTitle"/>
        <result column="goods_img" property="goodsImg"/>
        <result column="goods_detail" property="goodsDetail"/>
        <result column="goods_price" property="goodsPrice"/>
        <result column="goods_stock" property="goodsStock"/>
    </resultMap>


    <select id="queryAllGoodsVo" resultType="website.okunoda.secondtokill.VO.GoodsVo">
        select *
        from t_goods tg
                 left join t_seckill_goods tsg on tg.id = tsg.goods_id
    </select>
    <select id="queryById" resultType="website.okunoda.secondtokill.pojo.Goods">
        select *
        from t_goods tg
        where tg.id = #{id}
    </select>

    <select id="queryGoodsVoById" resultType="website.okunoda.secondtokill.VO.GoodsVo">
        select *
        from t_goods tg
                 left join t_seckill_goods tsg on tg.id = tsg.goods_id
        where tg.id = #{id}
    </select>

</mapper>
